# Assembly data

`BOM_JLCPCB.csv` は候補表です。LED D1–D12とESP32ソケットJ1/J2は、ユーザー手はんだ指定のため除外済みです。

この版の残り部品はTHT中心なので、SMT用CPL座標ではなくJLCPCBのwave/manual assembly工程を使います。`TBD_AT_ORDER`のLCSC番号を、注文日の実在庫・外形・耐圧・定格と照合してから置き換えてください。Q1 exact partを調達できない場合は勝手に代替せず、DNPまたはconsignedにします。

